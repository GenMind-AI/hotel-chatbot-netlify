[build]
  functions = "functions"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

[functions."chatbot"]
  included_files = ["functions/chatbot.py"]
  timeout = 30

[build.environment]
  NODE_VERSION = "20"
